
// Init Array with value
const initArray = (length: number) => {
    let arr = []
    for (let index = 0; index < length; index++) {
        arr.push(index)
    }
    return arr
}

export {initArray}